package rhythm.time

/**
  * Represents a simple time signature. Simple time signatures are typically
  * signatures where the beat can be subdivided into two equal beats (e.g. a
  * whole note into two half notes, a half note into two quarter notes, etc.)
  */
case class SimpleTimeSignature()
